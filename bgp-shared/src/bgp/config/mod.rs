/// This module contains all relevant type, structure and trait definitions to model
/// the complete BGP configuration
pub mod bgp_config;

/// This module contains all relevant type, structure and trait definitions to model
/// the configuration of a BGP4 peer
pub mod bgp_peer;